function multi(secondNumber) {
  const firstNumber = 27;
  if (secondNumber > 27) {
    let dif = secondNumber - firstNumber;
    let mul = dif * 2;
    console.log(mul);
  } else {
    console.log(firstNumber);
  }
}

multi(35);
multi(74);
multi(132);
